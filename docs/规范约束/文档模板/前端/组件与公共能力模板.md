# {{项目名}} 组件与公共能力

## 生成门禁

- 只收录被多个页面复用，或项目明确指定为公共入口的组件、hooks/composables、store、request client、utils。
- 页面私有组件不提升为公共能力；依赖中存在但项目未使用的组件不收录。

## 公共能力索引

| 分类 | 名称 | 路径 | 用途 | 主要使用方 | 稳定性 |
| --- | --- | --- | --- | --- | --- |
| UI/Hook/Store/Request/Util | `{{name}}` | `{{path}}` | {{用途}} | {{页面/模块}} | 稳定/内部 |

## 使用说明

### {{能力名称}}

- 适用场景：{{场景}}
- 不适用场景：{{边界}}
- 入口：`{{import path}}`
- 参数/Props：{{关键契约}}
- 事件/返回值：{{关键契约}}
- 状态与副作用：{{说明}}
- 示例来源：`{{真实使用路径:行号}}`
- 测试：`{{测试路径或未发现}}`

## 复用与新增规则

1. 新增同类能力前按名称、业务语义和 UI 形态检索本索引与源码。
2. 能扩展现有公共入口时不复制实现；扩展会破坏既有语义时才新增。
3. 改公共能力必须检查全部调用方并补回归测试。
