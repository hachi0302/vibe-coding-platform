# {{项目名}} 前端业务功能总览

> 只记录界面中已经实现且能够从路由、页面、状态或请求代码定位的功能。

## 生成门禁

- 仅前端或全栈项目生成。
- 不生成物理模型、后端 API 总览或后端状态机；仅记录前端实际使用的契约引用。
- 原型、设计稿和 README 只能作为补充材料，不能替代当前代码事实。

## 应用定位

- 目标用户：{{用户角色}}
- 使用场景：{{场景}}
- 应用边界：{{负责/不负责}}
- 入口：`{{main/router 路径}}`

## 页面与功能索引

| 业务域 | 页面/功能 | 路由 | 用户角色 | 核心组件 | 数据来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| {{业务域}} | {{功能}} | `{{route}}` | {{角色}} | `{{component}}` | `{{API/store}}` | 已实现/部分实现 |

## 页面与功能详情

### {{业务域}} / {{页面名称}}

- 用户目标：{{目标}}
- 路由与入口：`{{route}}`，`{{路径}}`
- 权限/可见条件：{{真实逻辑}}
- 核心交互：
  1. {{操作 → 状态变化 → 反馈}}
  2. {{操作 → 状态变化 → 反馈}}
- 数据来源：`{{API client/store/composable}}`
- 加载、空态、错误态：{{真实行为}}
- 关键复用组件：`{{组件}}`
- 代码证据：`{{路径:行号}}`
- 相关测试：`{{路径或未发现}}`

## 用户流程

```mermaid
flowchart LR
    A[{{入口}}] --> B[{{页面/操作}}]
    B --> C[{{结果/下一页}}]
```

## 前端使用的服务契约

| 页面/功能 | Client 方法 | 方法与路径 | 请求/响应类型 | 失败反馈 | 代码证据 |
| --- | --- | --- | --- | --- | --- |
| {{功能}} | `{{method}}` | `{{METHOD PATH}}` | `{{types}}` | {{反馈}} | `{{路径:行号}}` |

## 维护规则

- 路由、页面、权限、核心交互或接口依赖变化后同步本文。
- 计划功能进入详设，不得提前标记已实现。
