# {{项目名}} 业务功能总览

> 项目已实现业务能力的长期真源。新需求开始前先查本文，开发完成后必须同步。

## 生成门禁

- 只能收录能够从入口、服务实现、数据模型或测试定位的已实现能力。
- README 中的愿景、TODO、禁用代码和仅有空接口的功能不得标记“已实现”。
- 不存在的业务域不保留空章节；规划内容进入详设。

## 系统定位与边界

- 系统服务对象：{{用户/调用方}}
- 解决的问题：{{内容}}
- 核心业务范围：{{内容}}
- 明确不负责：{{内容}}

## 模块职责与业务链路

| 模块 | 业务职责 | 入口 | 核心服务 | 数据归属 | 证据 |
| --- | --- | --- | --- | --- | --- |
| `{{module}}` | {{职责}} | `{{Controller/Consumer}}` | `{{Service}}` | {{表/外部}} | `{{路径}}` |

```mermaid
flowchart LR
    U[{{调用方}}] --> E[{{入口}}]
    E --> B[{{业务服务}}]
    B --> S[{{数据库/第三方/消息}}]
```

## 业务功能索引

| 业务域 | 功能 | 使用方 | 入口 | 核心实现 | 数据/第三方 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| {{业务域}} | {{功能}} | {{使用方}} | `{{入口}}` | `{{类#方法}}` | {{依赖}} | 已实现/部分实现 |

## 业务功能详情

### {{业务域}}

#### {{功能名称}}

- 业务目的：{{用户价值}}
- 触发方：{{调用方/事件}}
- 前置条件：{{条件}}
- 入口：`{{API/Consumer/Job}}`
- 核心链路：`{{入口}} → {{Service}} → {{Repository/Client}}`
- 核心规则：
  1. {{真实规则及证据}}
  2. {{真实规则及证据}}
- 状态变化：{{状态与合法迁移；无状态则写不适用}}
- 幂等/并发/事务：{{真实机制；无证据写未发现}}
- 结果与副作用：{{落库、消息、外部调用}}
- 关键代码：`{{路径:行号}}`
- 相关测试：`{{路径}}`

## 接口全景索引

| 调用方 | 业务域 | 方法与路径/Topic | 功能 | 详细文档 |
| --- | --- | --- | --- | --- |
| {{调用方}} | {{业务域}} | `{{契约}}` | {{功能}} | [API/回调文档]({{相对路径}}) |

## 数据与状态全景

| 业务对象 | 权威模块 | 主表/存储 | 关键状态 | 更新入口 |
| --- | --- | --- | --- | --- |
| {{对象}} | {{模块}} | `{{table}}` | {{状态}} | `{{Service}}` |

## 外部系统与第三方能力

| 系统/机构 | 使用能力 | 调用模块 | 集成方式 | 详细文档 |
| --- | --- | --- | --- | --- |
| {{名称}} | {{能力}} | {{模块}} | HTTP/SDK/MQ | [说明]({{路径}}) |

## 已知边界与待确认事项

| 项目 | 事实 | 影响 | 证据 |
| --- | --- | --- | --- |
| {{边界}} | {{事实，不写猜测}} | {{影响}} | `{{路径/配置}}` |

## 维护规则

- 新增、删除或改变业务能力后同步功能索引和功能详情。
- 仅文档变化不改变“已实现”状态；必须以当前代码、配置和测试为准。
