# {{项目名}} 枚举值总览

## 生成门禁

- 仅后端或全栈项目生成，且必须从 enum、常量集合、配置字典或数据库字典中提取。
- 只记录有业务含义或对外契约意义的枚举；语言/框架内部枚举不收录。
- code、label、终态和迁移关系无法确认时写“待确认”，不得按名称猜测。

## 通用约定

| 项目 | 约定 | 证据 |
| --- | --- | --- |
| code/label 接口 | {{接口或无统一接口}} | `{{路径:行号}}` |
| 序列化方式 | {{name/code/value}} | `{{路径:行号}}` |
| 数据库存储方式 | {{字符串/数字/字典表}} | `{{路径:行号}}` |
| 未知值处理 | {{行为}} | `{{路径:行号}}` |
| 终态判定 | {{接口/集合/无统一约定}} | `{{路径:行号}}` |

## 枚举索引

| 分类 | 枚举 | 业务用途 | 使用模块 | 存储/传输值 | 代码位置 |
| --- | --- | --- | --- | --- | --- |
| {{分类}} | `{{EnumName}}` | {{用途}} | {{模块}} | code/name/value | `{{路径}}` |

## 枚举详情

### {{EnumName}} — {{中文名称}}

- 业务语义：{{说明}}
- 使用位置：{{接口/表字段/状态机}}
- 代码：`{{路径}}`

| 常量 | code/value | 中文含义 | 是否终态 | 允许迁移至 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `{{NAME}}` | `{{value}}` | {{含义}} | 是/否/不适用 | {{状态或不适用}} | {{约束}} |

## 跨层映射

| 枚举 | API 字段 | 数据库字段 | 前端/外部值 | 转换位置 |
| --- | --- | --- | --- | --- |
| `{{Enum}}` | `{{field}}` | `{{table.column}}` | `{{value}}` | `{{路径:行号}}` |

## 维护规则

- 新增、删除或重命名枚举值必须检查所有引用方、数据库存量值、接口调用方和状态迁移。
- 终态枚举必须明确不可逆约束；没有状态机的枚举不要强行补迁移关系。
