# {{项目名}} 物理模型总览

> 面向开发者的简明数据库说明，只回答“有哪些表、各表做什么、字段是什么”。

## 生成门禁

- 仅在项目存在关系型数据库表证据时生成：DDL、迁移脚本、ORM Entity/Model 或可访问的真实 schema。
- 纯前端项目、无关系型数据库项目禁止生成。
- 表、字段、类型和约束必须来自代码或数据库证据；仅从 SQL 查询片段看见表名时不得补全字段。
- 本文只保留表及字段事实，不展开接口关联、版本演进、发布步骤和猜测性 ER 关系。

## 数据库范围

| 数据源/schema | 使用模块 | 证据 | 可访问性 |
| --- | --- | --- | --- |
| `{{schema}}` | {{模块}} | `{{配置/迁移路径}}` | 已验证/仅配置存在/不可访问 |

## 表清单

| 表名 | 中文名称 | 用途 | 所属模块 | 结构证据 |
| --- | --- | --- | --- | --- |
| `{{table_name}}` | {{名称}} | {{一句话说明}} | {{模块}} | `{{DDL/Entity 路径}}` |

## 表字段

### `{{table_name}}` — {{中文名称}}

- 用途：{{业务用途}}
- 主键：`{{主键}}`
- 关键唯一约束：{{有则列出，无则写无代码证据}}
- 结构来源：`{{路径或数据库查询证据}}`

| 字段 | 类型 | 是否为空 | 含义 | 约束 |
| --- | --- | --- | --- | --- |
| `{{column}}` | `{{type}}` | 是/否 | {{业务含义}} | 主键/唯一/默认值/枚举/外键/普通 |

## 维护规则

- DDL、迁移脚本或 Entity 字段变化后同步本文。
- 同一字段在代码与数据库定义不一致时同时列出并标记风险，不替任一方兜底解释。
