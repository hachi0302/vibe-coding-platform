# {{项目名}} 回调接口总览

## 生成门禁

- 仅在代码中发现 webhook/callback/notify 等外部回调入口时生成。
- 必须核对路由、验签/鉴权、幂等、状态更新和响应代码；缺少任何一项都应如实标记风险。
- 定时任务、内部 MQ 消费者不是回调，不得混入本文。
- 未发现回调入口时禁止生成空模板。

## 通用约定

| 项目 | 内容 | 代码证据 |
| --- | --- | --- |
| 接收入口 | {{统一入口或分散入口}} | `{{路径:行号}}` |
| 验签/鉴权 | {{机制}} | `{{路径:行号}}` |
| 原始报文保留 | {{方式}} | `{{路径:行号}}` |
| 幂等键 | {{字段/组合}} | `{{路径:行号}}` |
| 成功响应 | {{内容}} | `{{路径:行号}}` |
| 失败与重试 | {{语义}} | `{{路径:行号}}` |

## 回调接口索引

| 来源 | 业务 | 方法 | 路径 | 验签 | 幂等 | 处理器 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| {{来源}} | {{业务}} | `POST` | `{{路径}}` | {{方式}} | {{幂等键}} | `{{Handler}}` | 已实现 |

## 回调详情

### {{来源}} — {{回调名称}}

- 业务目的：{{说明}}
- 入口：`{{METHOD PATH}}`
- 实现链路：`{{Controller}} → {{Service/Handler}} → {{持久化/MQ}}`
- 验签与解密顺序：{{按真实代码描述}}
- 幂等与终态保护：{{按真实代码描述}}
- 成功/失败响应：{{精确响应}}

**字段说明**

| 字段 | 类型 | 必填 | 含义 | 校验/转换 | 来源证据 |
| --- | --- | --- | --- | --- | --- |
| `{{字段}}` | `{{类型}}` | 是/否 | {{含义}} | {{处理}} | `{{路径:行号}}` |

**状态映射**

| 外部状态 | 内部状态 | 是否终态 | 后续动作 | 证据 |
| --- | --- | --- | --- | --- |
| `{{状态}}` | `{{状态}}` | 是/否 | {{动作}} | `{{路径:行号}}` |

## 请求与响应示例

> 只能使用脱敏后的真实结构或依据 DTO 生成的示例，禁止伪造业务值。

```json
{{脱敏请求示例}}
```

```text
{{成功响应示例}}
```

## 风险与维护

- 回调字段、验签方式、状态映射或响应格式变更时同步本文。
- 缺少验签、幂等或终态保护时必须列为明确风险，不能写成“框架已处理”。
