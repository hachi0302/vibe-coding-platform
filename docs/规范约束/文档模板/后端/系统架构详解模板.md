# {{项目名}} 系统架构详解

> 本文只描述代码库当前真实架构。规划中的架构进入详设，不得提前写入长期文档。

## 生成门禁

- 后端或全栈项目生成；必须完整扫描模块、构建文件、入口、配置和调用关系。
- 每个结论附代码或配置证据；无法确认的外部基础设施标记“仅检测到配置/不可访问”。
- 不写部署手册、服务器命令、值班流程等运维内容。

## 系统概览

- 系统定位：{{一句话定位}}
- 核心用户/调用方：{{列表}}
- 核心业务边界：{{范围}}
- 非职责：{{明确不负责的能力}}
- 主要入口：`{{入口文件/模块}}`

## 技术栈全景

| 层次 | 技术/版本 | 用途 | 证据 |
| --- | --- | --- | --- |
| 语言与运行时 | {{内容}} | {{用途}} | `{{构建文件}}` |
| Web/RPC 框架 | {{内容}} | {{用途}} | `{{路径}}` |
| 数据访问 | {{内容}} | {{用途}} | `{{路径}}` |
| 消息/缓存/任务 | {{内容}} | {{用途}} | `{{路径}}` |
| 测试 | {{内容}} | {{用途}} | `{{路径}}` |

## 模块与职责

| 模块 | 类型 | 入口 | 职责 | 依赖模块 | 禁止承载 |
| --- | --- | --- | --- | --- | --- |
| `{{module}}` | 应用/库/适配器 | `{{path}}` | {{职责}} | {{依赖}} | {{边界}} |

## 运行时拓扑与请求链路

```mermaid
flowchart LR
    A[{{调用方}}] --> B[{{入口模块}}]
    B --> C[{{业务模块}}]
    C --> D[{{数据库/第三方/消息}}]
```

| 链路 | 入口 | 核心处理 | 数据/外部依赖 | 返回/异步结果 |
| --- | --- | --- | --- | --- |
| {{链路}} | `{{入口}}` | `{{Service/UseCase}}` | {{依赖}} | {{结果}} |

## 分层与依赖方向

| 层次 | 职责 | 允许依赖 | 禁止依赖 | 代表路径 |
| --- | --- | --- | --- | --- |
| 接口层 | {{职责}} | {{允许}} | {{禁止}} | `{{路径}}` |
| 应用/业务层 | {{职责}} | {{允许}} | {{禁止}} | `{{路径}}` |
| 领域层（如有） | {{职责}} | {{允许}} | {{禁止}} | `{{路径}}` |
| 基础设施层 | {{职责}} | {{允许}} | {{禁止}} | `{{路径}}` |

## 公共能力与复用边界

| 能力 | 位置 | 使用场景 | 推荐入口 | 禁止重复实现 |
| --- | --- | --- | --- | --- |
| {{工具/基类/组件}} | `{{路径}}` | {{场景}} | `{{类#方法}}` | {{说明}} |

## 数据与状态边界

| 数据/聚合 | 权威模块 | 持久化位置 | 状态变化入口 | 一致性/并发策略 |
| --- | --- | --- | --- | --- |
| {{对象}} | {{模块}} | `{{表/存储}}` | `{{Service}}` | {{事务/锁/幂等/MQ}} |

## 同步与异步协作

| 类型 | 生产/调用方 | 消费/提供方 | 契约 | 可靠性语义 |
| --- | --- | --- | --- | --- |
| HTTP/RPC/MQ/任务 | {{模块}} | {{模块}} | `{{DTO/Topic/API}}` | {{超时/重试/幂等/事务}} |

## 安全与错误边界

| 关注点 | 当前实现 | 入口/证据 |
| --- | --- | --- |
| 身份认证 | {{内容}} | `{{路径}}` |
| 权限与数据隔离 | {{内容}} | `{{路径}}` |
| 输入校验 | {{内容}} | `{{路径}}` |
| 异常与错误码 | {{内容}} | `{{路径}}` |
| 敏感信息 | {{内容}} | `{{路径}}` |

## 构建与本地开发

| 项目 | 命令/说明 | 证据 |
| --- | --- | --- |
| 依赖安装 | `{{命令}}` | `{{构建文件}}` |
| 编译/构建 | `{{命令}}` | `{{构建文件}}` |
| 测试 | `{{命令}}` | `{{测试配置}}` |
| 本地启动 | `{{命令}}` | `{{入口/README}}` |

## 架构维护规则

- 新增模块、改变依赖方向、公共能力或核心链路后同步本文。
- 不把偶然实现写成强制规范；规范必须同时进入项目规则并能定位到稳定代码实证。
