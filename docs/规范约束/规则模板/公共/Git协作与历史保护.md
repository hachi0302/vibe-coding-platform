# Git 协作与历史保护

## 触发条件

查看或切换分支、创建工作区、暂存、提交、推送、合并、变基、拣选提交或处理冲突时必须阅读。

## 先读与复用优先

1. 先查看当前分支、工作区状态、远端跟踪关系、已有提交规范和项目实际 hooks。
2. 优先遵循仓库已经配置的分支策略、提交格式、检查脚本和发布流程；没有证据时不假设权限或流程。

## 强制规则

1. 开始任务先查看当前分支、工作区状态和上游；不覆盖、不还原、不提交用户已有改动。
2. 多任务并行时可使用 worktree；是否使用由用户选择，不能因小任务强制创建。
3. 未经用户明确授权，不执行 commit、push、rebase、merge、cherry-pick 或创建 PR/MR。
4. 用户选择提交时，只暂存本任务文件；commit message 遵守项目现有规范与历史提交风格。
5. push 被拒绝时先 fetch 并展示远端差异；不得使用强推。发生冲突时解释每个冲突的两侧语义，保护双方有效改动。
6. 禁止改写共享分支历史；禁止删除未知分支、worktree、stash 或未跟踪文件。

## 禁止事项

- 禁止使用破坏性命令处理未确认范围的改动。
- 禁止因推送被拒绝而跳过差异检查、强行覆盖远端或丢弃本地有效改动。
- 禁止把当前项目之外的分支策略、保护规则或发布权限写入本规则。

## 当前项目约定

| 项目 | 约定 | 证据 |
|---|---|---|
{{Git事实表}}

## 验证要求

- 提交前确认暂存区只包含本任务授权文件，且项目已有检查已按要求执行。
- 推送、合并或冲突处理后确认目标分支、远端提交和工作区状态符合用户授权范围。

## 待补信息

{{Git协作待补信息}}
