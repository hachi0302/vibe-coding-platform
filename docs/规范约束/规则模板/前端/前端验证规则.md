# 前端验证规则

## 最低验证

| 变化 | 必须验证 |
|---|---|
| 纯逻辑/hook | 单元测试：正常、边界、异常 |
| 组件 | 组件测试：渲染、交互、禁用/加载/错误状态 |
| 页面/路由 | 关键用户路径、权限、刷新/返回、空态与失败态 |
| API/类型 | 类型检查、请求参数、响应映射、错误处理 |
| 样式/布局 | 目标视口、长文本、空数据、主题/响应式（项目支持时） |

必须运行项目真实命令：`{{类型检查命令}}`、`{{lint命令}}`、`{{测试命令}}`、`{{构建命令}}`。不存在的命令从表中删除，不伪造。
