---
name: frontend-self-test
description: Use when前端页面、组件、状态管理、请求交互或样式行为发生变更，需要在交付前完成自动化与可视化验证时。
metadata:
  pattern: pipeline+reviewer
---

# 前端自测

## 项目资源

| 资源 | 当前项目位置/命令 |
|---|---|
| Agent 入口 | `{{Agent入口}}` |
| 前端总览 | `{{项目总览路径}}` |
| 前端规则 | `.claude/rules/前端/` |
| 组件与公共能力 | {{项目证据路径}} |
| 测试目录与框架 | {{测试证据路径}} |
| lint | `{{lint命令}}` |
| 类型检查 | `{{类型检查命令}}` |
| 测试 | `{{测试命令}}` |
| 构建 | `{{构建命令}}` |

## 测试矩阵

| 层次 | 必测内容 |
|---|---|
| 逻辑 | 工具函数、状态转换、校验、权限判断 |
| 组件 | 渲染、事件、空态、加载、成功、失败、禁用态 |
| 集成 | 路由、请求参数、响应映射、缓存/状态管理 |
| 回归 | 原 Bug case、受影响页面和公共组件调用方 |
| 工程 | `{{lint命令}}`、`{{typecheck命令}}`、`{{test命令}}`、`{{build命令}}` |

## 执行流程（Pipeline）

1. 读取前端规则、组件规范和详设验收项。
2. 先补能失败的测试，再实现修复或功能。
3. 使用项目已有测试框架和 mock 方式；优先测用户可见行为，不锁死内部实现。
4. 对关键页面做真实运行验证，记录视口、步骤和截图/结果；无浏览器能力时明确缺口。
5. 汇报测试文件、真实命令、通过数量、失败处置和遗留盲区。

## 可视化验收 Gate

- 使用当前项目实际启动方式打开目标页面，而不是孤立 HTML mock。
- 核对正常、加载、空态、失败、无权限、禁用和重复提交。
- 核对项目支持的主题、长文本、目标视口、键盘和可访问性。
- 截图只能证明展示，交互结果还必须有操作步骤、请求/状态与自动化证据。

## 失败处理

测试失败不得更新快照、删除断言或扩大 mock 范围求绿；必须先判断是 production 缺陷、测试错误还是环境阻断，并记录处置。

## 完成 Gate

测试路径、case 矩阵、真实命令、通过数量、可视化结果、失败处置与测试文件去留全部可复查后才算完成。

编译成功不等于交互正确；快照通过不等于关键状态已覆盖。
