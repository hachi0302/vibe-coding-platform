---
name: code-review
description: 当用户要求审查代码、diff、合并请求、实现质量，或在功能交付前检查正确性与回归风险时使用。
metadata:
  pattern: reviewer
---

# 审查代码

## 触发条件

- 用户要求 code review、审查 diff、检查合并请求或交付前复核。
- 已存在可读取的需求、详设、代码或暂存区改动；不能把未展示的内容当作已审查。

## 项目资源

- 项目入口与核心约束：`{{Agent入口}}`
- 规则索引：`.claude/rules/README.md`
- 项目总览与架构：`{{项目总览路径}}`
- 测试、构建和静态检查：`{{测试命令}}`、`{{构建命令}}`
- 源码与公共能力：{{项目证据路径}}
- 测试位置：{{测试证据路径}}

## 执行步骤（Reviewer）

1. 读取需求/详设与项目规则，确认改动应解决什么问题。
2. 查看完整 diff 和调用链，不只看被点名文件。
3. 优先检查正确性：状态、金额/精度、权限、并发、幂等、异常、资源释放和兼容性。
4. 检查是否复用已有组件与公共能力，是否出现重复实现或跨层职责。
5. 检查测试能否证明行为：正常、边界、异常、原 Bug case 与关键回归。
6. 检查文档同步、敏感信息和无关改动。

## 输出契约

先列问题，按严重度排序；每项包含文件位置、触发条件、影响、证据和建议。没有问题时明确说明仍存在的测试盲区或未验证事实。

| 级别 | 判定 |
|---|---|
| 阻断 | 会造成错误结果、安全/数据风险或不可接受回归 |
| 重要 | 在明确场景下出现缺陷或维护风险 |
| 建议 | 不影响正确性但可提升清晰度 |

不要用风格偏好冒充缺陷，不要求与本任务无关的重构。

## 禁止事项

- 禁止把个人风格偏好、未经复现的猜测或未展示代码写成缺陷事实。
- 禁止要求与当前改动无关的重构；禁止以“可能有问题”为由给出阻断结论。
- 禁止使用项目中不存在的命令、框架、规则或历史案例。

## 可执行验证

- 仅执行项目资源中已确认存在的静态检查、测试或构建命令；记录命令、范围、退出码和未执行原因。
- 对每个阻断/重要 finding 给出可触发条件和受影响调用链；不能复现或无法证实的内容进入“待补信息”。

## 待补信息

| 缺失信息 | 当前已确认事实 | 影响 | 下一步证据 |
|---|---|---|---|
| {{缺失需求/详设/diff/环境}} | {{可定位的已有材料}} | {{无法确认的审查范围}} | {{应读取的文件、命令或用户材料}} |

## 证据要求

每个 finding 必须包含：文件与位置、触发输入/状态、错误行为、为什么现有测试未覆盖、最小修复方向。不能指出可触发场景的问题不列为阻断项。

## 完成 Gate

- 已读需求/详设而不是只看 diff。
- 已沿入口和共享调用方检查影响面。
- 已核对项目规则、错误/权限/数据/状态约束和测试证据。
- Findings 按严重度排序且没有把推测写成事实。
- 无 finding 时仍列出未执行测试或不可访问环境造成的盲区。

## 失败处理

- 缺需求/详设：先基于明确契约审查正确性，并把无法判断的业务项列为盲区。
- diff 不完整：不对未展示代码作全量通过结论。
- 测试无法运行：保留静态 finding，并明确动态行为尚未验证。
