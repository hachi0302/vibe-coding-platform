---
name: backend-log-diagnose
description: Use when后端问题需要读取本地文件、容器、集中日志或已授权远程日志，并结合请求标识、业务号或时间窗口定位错误链路时。
metadata:
  pattern: pipeline
---

# 使用日志诊断后端问题

## 项目资源

| 资源 | 当前项目位置/状态 |
|---|---|
| Agent 入口 | `{{Agent入口}}` |
| 项目总览 | `{{项目总览路径}}` |
| 日志规则与脱敏约束 | `{{日志规则路径}}` |
| 本地日志 | {{本地日志路径与检索命令}} |
| Docker/容器日志 | {{容器日志命令或不适用}} |
| Kubernetes | {{context/namespace/服务/container 或未配置}} |
| 集中日志 | {{Loki/ELK/Grafana/云日志入口或未配置}} |
| 已授权远程日志 | {{SSH/API/CLI 入口或未配置}} |

只登记代码、配置或安全只读探测证实的来源。未配置来源必须写“有证据但需配置”或“不适用”，不得假装可访问。

## 执行流程

1. 确认环境、时间窗口、服务、请求 ID/业务号和敏感信息边界。
2. 先用最窄条件检索，再围绕首个异常向前后扩展上下文。
3. 将日志时间线映射到入口、方法、数据状态和外部调用。
4. 必要时结合只读数据库证据；无授权不连接、不猜测。
5. 输出日志证据、代码证据、数据证据及结论可信度。

禁止在报告中泄露 token、密钥、完整证件号、银行卡号或用户隐私。

## 完成 Gate

- 已说明环境、服务、时间窗口和检索条件。
- 每项结论能定位到日志片段与 `{{源码路径}}`，没有把相关性写成因果。
- 已区分“已确认根因”和“当前推测”，并记录不可访问的日志来源。
- 输出已脱敏，未暴露凭证、个人信息或完整业务敏感数据。

## 失败处理

- 日志源未配置或无权限：保留真实错误与接入条件，继续完成代码可证明的部分。
- 检索无结果：核对环境、时区、实例、标识和采集范围，不扩大成无边界全量扫描。
- 多实例时间线冲突：按请求 ID/业务号重建顺序，无法确认时不得给确定根因。
